# Expected-failure (xfail) cases from tests/ok.lst.
# Empty for now: the "{n,}"-quantifier and group+quantifier bugs previously
# tracked here were fixed (re_compile_to()'s "{n,}" sscanf validation, and
# matchpattern()'s GROUPEND-vs-quantifier-lookahead ordering). Keep this file
# as the harness's known-bugs list for the next one; a natural failure here
# is tolerated, an unexpected pass (XPASS) fails CI.
